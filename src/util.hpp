#ifndef UTIL_HPP_ 
#define UTIL_HPP_

#include <cstdio>

#define maxK 100
#define maxL 1000

/* Maximum allowed total genome length & num genomes & num contigs.*/
//static const uint64_t maxN = UINT64_MAX >> 28;
//static const uint32_t maxM = UINT32_MAX >> 4;
//static const uint32_t maxC = UINT32_MAX >> 4;
#define maxN (UINT64_MAX >> 28)
#define maxM (UINT32_MAX >> 12)
#define maxC (UINT32_MAX >> 4)

#endif
